name: Plugin compatibility

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: plugin-compatibility-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: mindroom-ai/mindroom/.github/workflows/plugin-ci.yml@6868d8bf005036301b705950c7112f688d340dd8
    with:
      plugin_repository: ${{ github.repository }}
      plugin_ref: ${{ github.sha }}
      artifact_name: voice-enrich-plugin
      # Keep aligned with the reusable workflow SHA above.
      mindroom_ref: 6868d8bf005036301b705950c7112f688d340dd8
      require_tests: true
